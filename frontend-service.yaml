apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: mechat
    tier: frontend
spec:
  type: LoadBalancer
  ports:
    # the port that this service should serve on
  - port: 80
  selector:
    app: mechat
    tier: frontend